using System.Drawing; // Zorg ervoor dat je System.Drawing namespace gebruikt voor kleuren

namespace InDeBanVanDeRing
{
    public static class CardColors
    {
        public static readonly Color Grey = Color.FromKnownColor(KnownColor.Gray);
        public static readonly Color White = Color.FromKnownColor(KnownColor.ControlLightLight);
    }
}
